<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Arahkan semua request ke folder public/ (kecuali file/folder yang sudah ada)
    RewriteCond %{REQUEST_URI} !^public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
