---
deployment:
  tasks:
    # Deploy langsung ke public_html/ (Opsi A - simple & reliable)
    # mail.wanseven.com/ folder tidak terpengaruh karena tidak ada di repo ini
    - export DEPLOYPATH=/home/$USER/public_html
    - /bin/cp -Rp . $DEPLOYPATH
